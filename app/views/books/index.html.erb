<!DOCTYPE html>
<html>
<head>
  <title>Book Collection</title>
</head>
<body>
  <h1>Book Collection: Home Page</h1>

  <% if notice %>
    <div style="background-color: #d4edda; padding: 10px; margin: 10px 0; border: 1px solid #c3e6cb;">
      <%= notice %>
    </div>
  <% end %>

  <table border="1" cellpadding="10">
    <thead>
      <tr>
        <th>Title</th>
        <th>Author</th>
        <th>Price</th>
        <th>Published Date</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= book.title %></td>
          <td><%= book.author %></td>
          <td>$<%= number_with_precision(book.price, precision: 2) %></td>
          <td><%= book.published_date.strftime("%B %d, %Y") if book.published_date %></td>
          <td>
            <%= link_to "Show", book_path(book) %> |
            <%= link_to "Update", edit_book_path(book) %> |
            <%= link_to "Delete", book_path(book), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete the book?" } %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= link_to "Add New Book", new_book_path, style: "display: inline-block; padding: 10px; background-color: #0077b6; color: white; text-decoration: none; margin-top: 30px" %>
</body>
</html>
