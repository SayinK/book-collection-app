<!DOCTYPE html>
<html>
<head>
  <title>Book Details</title>
</head>
<body>
  <h1>Book Details</h1>


  <p><strong>Title:</strong> <%= @book.title %></p>
  <p><strong>Author:</strong> <%= @book.author %></p>
  <p><strong>Price:</strong> $<%= number_with_precision(@book.price, precision: 2) %></p>
  <p><strong>Published Date:</strong> <%= @book.published_date.strftime("%B %d, %Y") if @book.published_date %></p>


  <div style="margin-top: 20px;">
    <%= link_to "Edit", edit_book_path(@book) %> |
    <%= link_to "Delete", book_path(@book), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete the book?" } %> |
    <%= link_to "Back to Home Page", root_path %>
  </div>
</body>
</html>
